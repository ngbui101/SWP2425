import { ref } from 'vue';

export const tour1 = ref(null);  // This ref will be used in LogoHeader and TourController
export const tour2 = ref(null);  // This ref will be used in TourController
export const tour3  = ref(null);  // This ref will be used in TourController
export const tour4 = ref(null);  // This ref will be used in TourController